<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Bartenda Home</title>
    <link rel="stylesheet" type="text/css" href="/../../css/index-styles.css">
    <link href="https://fonts.googleapis.com/css?family=Aclonica" rel="stylesheet">
  </head>
  <body>
    <header>
      <video playsinline autoplay muted loop poster="./images/whiskey.png" id="headerVid">
          <source src="../video/whiskey.webm" type="video/webm">
          <source src="../video/whiskey.mp4" type="video/mp4">
      </video>
      <h1 class="title">@BARTENDA 21</h1>
      <h3 class="subtitle">DISCOVER & SHARE</h3>
    </header>
    <nav>
      <a href='/login'>log in</a>
      <a href='#'>register</a>
      <a href='#'>about</a>
    </nav>
    <main>
      <p><a href='/bartenda/new'>Add a drink to the bar</a></p>
      <div class='col1'></div>
      <div class='col2'>
        <% for (let post of allPosts) { %>
          <a href='/bartenda/<%= post._id %>'><img src='<%= post.url %>'></a>
          <h3><%= post.drinkName %></h3>
          <h4><%= post.submittedBy %></h4>
        <% } %>
      </div>
    </main>
    <footer>
    </footer>
  </body>
</html>
