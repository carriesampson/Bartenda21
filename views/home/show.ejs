<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Post Details</title>
  </head>
  <body>
    <header>
      <h1>BARTENDA 21</h1>
      <h3>DISCOVER & SHARE</h3>
    </header>
    <nav>
      <a href="#">log in</a>
      <a href="#">register</a>
      <a href="#">about</a>
    </nav>
    <main>

      <p><a href='/bartenda/new'>Add a drink to the bar</a></p>
      <p><a href='/bartenda/<%= onePost.id %>/edit'>Edit your post</a></p>
      <form action="/bartenda/<%=onePost.id%>?_method=DELETE" method="POST">
        <button>DELETE</button>
      </form>
      <div class="mainPost">
        <img src="<%= onePost.url %>">
        <h3><%= onePost.drinkName %></h3>
        <h4><%= onePost.submittedBy %></h4>
        <p><%= onePost.ingredients %></p>
        <p><%= onePost.locationName %></p>
        <p><%= onePost.locationCity %></p>
        <p><%= onePost.locationState %></p>
        <p><%= onePost.price %></p>
        <p><%= onePost.yourComments %></p>
      </div class="comments">
        <% for (let comment of comments) { %>
          <h4><%= comment.submittedBy %></h4>
          <p><%= comment.content %></p>
        <% } %>
      </div>
      <div class="submitComment">
          <form action="/comments" method="POST">
            <h3>Submit a comment</h3>
            <p>Enter your name:</p>
            <input type="text" name="submittedBy" placeholder="Your name"/>
            <p>Enter your comment:</p>
            <input type="text" name="content" placeholder="Your comment"/>
            <input type="hidden" name="post" value="<%= onePost._id %>"/>
            <p><button>Submit</button></p>
          </form>
        </div>
    </main>
    <footer>
    </footer>

  </body>
</html>
